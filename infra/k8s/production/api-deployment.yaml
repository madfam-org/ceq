apiVersion: apps/v1
kind: Deployment
metadata:
  name: ceq-api
  namespace: ceq
  labels:
    app: ceq-api
    app.kubernetes.io/name: ceq-api
    app.kubernetes.io/part-of: ceq
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ceq-api
  template:
    metadata:
      labels:
        app: ceq-api
        app.kubernetes.io/name: ceq-api
        app.kubernetes.io/part-of: ceq
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
      - name: ghcr-credentials
      containers:
      - name: ceq-api
        image: ghcr.io/madfam-org/ceq/api:latest
        ports:
        - containerPort: 5800
          name: http
        envFrom:
        - secretRef:
            name: ceq-secrets
            optional: true
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
        startupProbe:
          httpGet:
            path: /health
            port: 5800
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 5800
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 5800
          periodSeconds: 30
